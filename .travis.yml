language: cpp

compiler: gcc

os: linux

sudo: required

dist: trusty

script:
  - cd ./test/
  - qmake -makefile -Wall
  - make
  - bash <(curl -s https://codecov.io/bash) 

branches:
    only:
    - dev
    - /^feature/*$\
    - /^refactor/*$\
    - /^fix/*$\

notifications:
    email: false

