language: cpp

compiler: gcc

os: linux

sudo: required

dist: bionic

script:
  - cd ./TestLib/
  - mkdir build && cd build
  - cmake && make
  
# after success:
#  - bash <(curl -s https://codecov.io/bash) 

branches:
    only:
    - dev
    - /^feature/*$\
    - /^refactor/*$\
    - /^fix/*$\

notifications:
    email: false

