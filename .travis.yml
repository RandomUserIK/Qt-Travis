language: cpp

compiler: gcc

os: linux

sudo: required

dist: trusty

script:
  - cd ./test/
  - qmake -Makefile -Wall
  - make

branches:
    only:
    - dev
    - /^feature/*$\
    - /^refactor/*$\
    - /^fix/*$\

notifications:
    email: false

